# GRUAtt-Autoformer
The proposed model adopts an encoder–decoder framework, preserving the advantages of Auto-Correlation in capturing global dependencies, while enhancing the extraction of local temporal features through a bidirectional GRU-Attention (BiGRU-Attention) mechanism.
