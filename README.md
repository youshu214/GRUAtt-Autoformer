# GRUAtt-Autoformer
The proposed model adopts an encoder–decoder framework, preserving the ad-vantages of Auto-Correlation in capturing global dependencies, while enhancing the extraction of local temporal features through a bidirectional GRU-Attention (BiGRU-Attention) mechanism.
